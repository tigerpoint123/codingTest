package algorithm;

public class InsertionSort {
    public static void main(String[] args) {
        int[] init = {5, 3, 42, 2, 10, 26, 35, 15, 47};

        for (int index = 1; index < init.length; index++) {
            int temp = init[index];
            int prev = index - 1;

            while( (prev >= 0) && (init[prev] > temp )) {
                
            }
        }
    }
}
